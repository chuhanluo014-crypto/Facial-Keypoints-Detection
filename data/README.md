# Data

Raw Kaggle-style CSV files are stored in `data/raw/` and tracked through Git LFS.

Keep raw files immutable. Put cleaned, cached, or feature-engineered files under `data/processed/` and generated predictions under `outputs/`.
